{
  "name": "@afterbuy/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --exclude ../../node_modules --exclude dist --exclude ../../.afterbuy-data src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@afterbuy/core": "*",
    "@afterbuy/db": "*",
    "@fastify/cors": "^11.3.0",
    "fastify": "^5.12.1",
    "linkedom": "^0.18.13",
    "zod": "^4.5.4"
  }
}
